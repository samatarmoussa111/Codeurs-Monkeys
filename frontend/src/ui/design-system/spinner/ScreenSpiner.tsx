import Spinner from "./Spinner";

const ScreenSpiner = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <Spinner size="large" />
    </div>
  );
};

export default ScreenSpiner;
